#!/bin/sh
set -eu

repo_root="$(git rev-parse --show-toplevel 2>/dev/null || exit 0)"
project_hook="$repo_root/scripts/hooks/pre-commit"

if [ -f "$project_hook" ]; then
  exec sh "$project_hook" "$@"
fi

exit 0
